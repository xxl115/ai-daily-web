# AI Daily Web - Product Hunt 风格重设计

**日期**: 2025-02-09
**目标**: 提升 UX，采用 Product Hunt 的简洁风格

## 设计目标

- 参考 Product Hunt 的视觉风格
- 简洁、紧凑的信息密度
- 保留所有现有核心功能
- 默认显示今日，支持切换时间维度

---

## 1. 整体布局

### 页面结构（三栏布局）

```
┌─────────────┬──────────────────────┬─────────────┐
│  左侧导航   │    主内容区          │  右侧统计   │
│  (240px)    │    (max 760px)       │  (320px)    │
│             │                      │             │
│ - 今日      │  [搜索栏]            │  [统计卡片] │
│ - 昨日      │  [时间标签]          │  [订阅框]   │
│ - 本周      │                      │             │
│ - 上月      │  [文章列表]          │             │
│             │                      │             │
│ [来源统计]  │                      │             │
│ [热门Top5]  │                      │             │
└─────────────┴──────────────────────┴─────────────┘
```

### 左侧栏 (240px)
- 导航菜单（今日、昨日、本周、上月）
- 数据来源统计（Top 10 来源及文章数）
- 热门文章 Top 5（小卡片）

### 中间主内容区 (max 760px)
- 顶部搜索栏（全宽）
- 时间段标题（"今日热门"）
- 文章列表（单列紧凑卡片）

### 右侧栏 (320px)
- 统计数据卡片
- Newsletter 订阅框（可选）

### 响应式设计

| 屏幕尺寸 | 布局 |
|---------|------|
| >1280px | 完整三栏 |
| 768-1280px | 左侧折叠为汉堡菜单，右侧移到底部 |
| <768px | 单栏，侧边栏作为抽屉 |

---

## 2. 文章卡片设计

### 卡片结构

```
┌──────────────────────────────────────────────────┐
│ [图标] 标题 ..................................[热度]│
│        来源 · 时间 ............................[AI]│
└──────────────────────────────────────────────────┘
```

### 元素布局

| 位置 | 元素 | 尺寸 | 说明 |
|------|------|------|------|
| 左侧 | 来源图标 | 32x32px | 真实 logo（simple-icons） |
| 中间上 | 标题 | 16px | 可点击，新标签打开 |
| 中间下 | 元信息 | 12px | 来源 · 时间 |
| 右侧 | 热度 | 大号数字 | 带火焰 emoji 🔥 |
| 右侧 | AI 按钮 | 20px | 图标按钮 |

### 卡片样式

```css
background: #FFFFFF
border: 0.5px solid #E5E7EB
border-radius: 6px
padding: 10px
gap: 6px

hover: box-shadow + translateY(-1px)
```

### 来源图标

使用 `simple-icons` 库：

| 来源 | 图标 |
|------|------|
| Hacker News | Y Combinator logo |
| GitHub | GitHub logo |
| V2EX | V2EX favicon |
| Product Hunt | PH logo |
| OpenAI | OpenAI logo |
| Dev.to | Dev.to logo |
| TechCrunch | TC logo |
| The Verge | Verge logo |
| 其他 | 统一默认图标 |

---

## 3. 配色方案

### 颜色系统

```css
/* 品牌色 */
--brand: #FF6B4A;

/* 背景 */
--bg-page: #F9FAFB;
--bg-card: #FFFFFF;
--bg-hover: #F3F4F6;

/* 文字 */
--text-primary: #111827;
--text-secondary: #6B7280;
--text-muted: #9CA3AF;

/* 边框 */
--border: #E5E7EB;
```

### 来源强调色

仅保留主要来源的颜色，其他统一：

```css
--source-hn: #FF6600;
--source-github: #24292E;
--source-ph: #DA552F;
--source-default: #6B7280;
```

### 字体

```css
--font-display: 'Outfit', sans-serif;
--font-body: 'Plus Jakarta Sans', sans-serif;
```

---

## 4. 交互设计

### 搜索与筛选

**搜索栏:**
- 固定顶部，全宽
- 实时搜索（输入即过滤）
- 显示结果数量

**来源筛选:**
- 横向滚动药丸按钮
- 显示每个来源的文章数量
- 选中状态高亮

### 时间维度切换

**顶部标签:**
```
[今日热门] [昨日热门] [本周热门] [上月热门]
```
- 点击切换
- 内容淡入淡出
- 默认：今日热门

### 卡片交互

| 元素 | 行为 |
|------|------|
| 标题点击 | 新标签页打开原文 |
| AI 按钮点击 | 展开/折叠摘要 |
| 卡片 hover | 轻微阴影 + 上移 1px |

### AI 摘要

- 在卡片下方展开
- 显示加载状态
- 支持复制摘要

---

## 5. 技术实现

### 新增组件

| 组件 | 路径 | 功能 |
|------|------|------|
| ArticleCardCompactPH | `components/article/` | PH 风格紧凑卡片 |
| ArticleListPH | `components/article/` | PH 风格列表容器 |
| TimeNav | `components/layout/` | 时间切换导航 |
| SourceFilterPills | `components/filters/` | 来源筛选药丸 |
| SourceIcon | `components/icons/` | 来源图标组件 |

### 数据获取

**使用 React Query:**

```typescript
// 缓存每个时间段的数据
useQuery(['articles', period], () => fetchArticles(period), {
  staleTime: 5 * 60 * 1000, // 5分钟
  cacheTime: 10 * 60 * 1000,
})
```

**API 调整:**

```typescript
// 添加时间段参数
GET /api/hotspots?period=today|yesterday|week|month

// 响应
{
  success: true,
  total: 150,
  sources: [...],
  data: [...]
}
```

### 图标方案

**安装:**

```bash
npm install simple-icons
```

**使用:**

```typescript
// components/icons/SourceIcon.tsx
import * as SimpleIcons from 'simple-icons';

const iconMap: Record<string, string> = {
  'Hacker News': 'ycombinator',
  'GitHub': 'github',
  'Product Hunt': 'producthunt',
  // ...
};

export function SourceIcon({ source }: { source: string }) {
  const iconName = iconMap[source];
  const icon = SimpleIcons[iconName];
  return <img src={`data:image/svg+xml,${icon.svg}`} />;
}
```

### 状态管理

**时间维度状态:**

```typescript
// hooks/useTimePeriod.ts
const [period, setPeriod] = useState<Period>('today');
```

**来源筛选状态:**

```typescript
// hooks/useSourceFilter.ts
const [selectedSource, setSelectedSource] = useState<string | null>(null);
```

---

## 6. 实现优先级

### P0 (核心)
- [ ] ArticleCardCompactPH 组件
- [ ] ArticleListPH 组件
- [ ] TimeNav 时间切换
- [ ] 首页布局重构

### P1 (重要)
- [ ] SourceIcon 组件
- [ ] SourceFilterPills 组件
- [ ] React Query 集成
- [ ] API 时间段参数

### P2 (优化)
- [ ] 左右侧边栏
- [ ] 响应式适配
- [ ] 加载状态优化
- [ ] AI 摘要交互

---

## 7. 待确认问题

1. ~~是否显示排名？~~ → 不显示
2. ~~来源图标方案？~~ → 使用真实 logo
3. ~~时间维度组织方式？~~ → 默认今日 + 可切换
4. 是否需要 Newsletter 订阅功能？
5. 是否需要保存用户偏好（localStorage）？

---

## 8. 设计参考

- **Product Hunt**: https://www.producthunt.com
- **核心特点**: 简洁、紧凑、信息密度高
- **保持元素**: 现有品牌色和字体系统
